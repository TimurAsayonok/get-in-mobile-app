import { StyleSheet } from 'react-native';

import { scale, verticalScale, moderateScale } from 'utils/scale';

const TitleComponentStyles = StyleSheet.create({
  titleContainer: {
    marginVertical: verticalScale(10)
  },
  titleText: {}
});

export { TitleComponentStyles };